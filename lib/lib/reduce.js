/**
 * Given a Array of concentrations, with an pair array of dates, and returns an
 * Array of the last twelve hourly concentrations.
 * @param  {Array} arr   Array of concentrations
 * @param  {Array} dates Array of dates
 * @return {Array}       Arrays of 12 items
 */
module.exports = (arr, dates) => {
  return dates
}
